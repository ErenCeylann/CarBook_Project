@model List<ResultLocationDto>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/AdminLayout/Index.cshtml";
    int count = 0;
}

<div class="page-content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-xl-12">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title">Lokasyon Alanı</h4>
                        <p class="card-subtitle mb-4">
                           Lokasyon bilgilerinin yer aldığı sayfadır.Bu sayfa üzerinden yeni bir lokasyon eklenebilir,silinebilir, veya güncellenebilir.
                        </p>

                        <div class="table-responsive">
                            @if (Model.Count > 0)
                            {
                                <table class="table mb-0">
                                    <thead>
                                        <tr>
                                            <th>#</th>
                                            <th>Lokasyon Adı</th>
                                            <th>Güncelle</th>
                                            <th>Sil</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @foreach (var item in Model)
                                        {
                                            count++;
                                            <tr>
                                                <th scope="row">@count</th>
                                                <td>@item.Name</td>
                                                <td><a href="/Admin/AdminLocation/UpdateLocation/@item.LocationId" class="btn btn-outline-success">Güncelle</a></td>
                                                <td><a href="/Admin/AdminLocation/RemoveLocation/@item.LocationId" class="btn btn-outline-danger">Sil</a></td>

                                            </tr>
                                        }
                                    </tbody>
                                </table>
                            }
                            else
                            {
                                <div class="alert alert-danger" role="alert">
                                    <h4 class="alert-heading">Lokasyon Bulunamadı!</h4>
                                    <p>Şu anda Lokasyon bulunmamaktadır.Yeni lokasyob eklemek için aşşağıdan lokasyon girişi butonuna tıklayınız.</p>
                                </div>
                            }
                            <a href="/Admin/AdminLocation/CreateLocation" class="btn btn-outline-info">Lokasyon Girişi</a>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
</div>